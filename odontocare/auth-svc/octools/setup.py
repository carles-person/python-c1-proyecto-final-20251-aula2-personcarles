from setuptools import setup, find_packages

setup(
    name="octools",
    version = "0.1.0",
    description="Shared tools for OdontoCare",
    packages=find_packages(),
    python_requires=">=3.10",
    author="C.Person",
)